## store_keeper data 







  i need you create the project for me useing react vite and tailwind css on frontend and nodejs express and mongodb on backend for the gym booking system where user will book session and recieve email with qr codes to be scanned and after qr code scanned get expired then use jwt for authothication and authorization  so that email must be send after user fullfils credentials which is name , email , and phone contact and confirm payment 



git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/JoshTalent/gspstore.git
git push -u origin main